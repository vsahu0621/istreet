// import 'package:flutter/material.dart';

// class AppColors {
//   // Primary Theme Colors
//   static const primaryBlue = Color(0xFF0A84FF); // Light Blue (Apple style)
//   static const primaryLightBlue = Color(0xFFE6F2FF);
//   static const secondaryBlue = Color(0xFF5DB8FF);
//   static const iStreetBlue = Color(0xFF4287F5);
//   // Background Colors
//   static const background = Color(0xFFF8FBFF); // Very light blue-white
//   static const cardBackground = Colors.white;

//   // Text Colors
//   static const textDark = Color(0xFF1A1A1A);
//   static const textLight = Color(0xFF6F6F6F);

//   // Success / Danger
//   static const successGreen = Color(0xFF16C784);
//   static const dangerRed = Color(0xFFEA3943);

//   // Borders
//   static const border = Color(0xFFD8E6F5); // Light blue-gray
// }

import 'package:flutter/material.dart';

class AppColors {
  // --------------------------------------------------
  // PRIMARY BRAND COLORS
  // --------------------------------------------------
  static const primaryBlue = Color(0xFF0A84FF);
  static const secondaryBlue = Color(0xFF5DB8FF);
  static const iStreetBlue = Color(0xFF4287F5);

  // Used in hero / CTA gradients
  static const deepBlue = Color(0xFF1E40AF);
  static const indigo = Color(0xFF6366F1);
  static const violet = Color(0xFF8B5CF6);

  // --------------------------------------------------
  // BACKGROUND & SURFACE
  // --------------------------------------------------
  static const background = Color(0xFFF8FAFC);
  static const cardBackground = Colors.white;

  // --------------------------------------------------
  // TEXT COLORS
  // --------------------------------------------------
  static const textDark = Color(0xFF0F172A);
  static const textLight = Color(0xFF64748B);
  static const textWhite = Colors.white;

  // --------------------------------------------------
  // STATUS / ACCENT COLORS
  // --------------------------------------------------
  static const successGreen = Color(0xFF10B981);
  static const successGreenDark = Color(0xFF059669);

  static const infoBlue = Color(0xFF3B82F6);
  static const infoBlueDark = Color(0xFF2563EB);

  static const warningYellow = Color(0xFFFBBF24);
  static const dangerRed = Color(0xFFEF4444);

  static const purple = Color(0xFFA855F7);
  static const purpleDark = Color(0xFF9333EA);

  // --------------------------------------------------
  // STATS COLORS
  // --------------------------------------------------
  static const statsBlue = Color.fromRGBO(8, 109, 232, 1);
  static const statsGreen = Color(0xFF4ADE80);
  static const statsPurple = Color(0xFFA78BFA);

  // --------------------------------------------------
  // BORDERS & SHADOW
  // --------------------------------------------------
  static const border = Color(0xFFD8E6F5);
}
