import 'package:flutter/material.dart';

class AppColors {
  // Primary Theme Colors
  static const primaryBlue = Color(0xFF0A84FF); // Light Blue (Apple style)
  static const primaryLightBlue = Color(0xFFE6F2FF);
  static const secondaryBlue = Color(0xFF5DB8FF);
  static const iStreetBlue = Color(0xFF4287F5);
  // Background Colors
  static const background = Color(0xFFF8FBFF); // Very light blue-white
  static const cardBackground = Colors.white;

  // Text Colors
  static const textDark = Color(0xFF1A1A1A);
  static const textLight = Color(0xFF6F6F6F);

  // Success / Danger
  static const successGreen = Color(0xFF16C784);
  static const dangerRed = Color(0xFFEA3943);

  // Borders
  static const border = Color(0xFFD8E6F5); // Light blue-gray
}
